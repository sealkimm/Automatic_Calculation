# Automatic Calculation

브랜드와 디바이스 기준에 맞춰 `px` 값을 `vw` 또는 `clamp()` 값으로 변환해주는 간단한 웹 계산기입니다.

## Features

- `Aveda`, `Mac` 브랜드 선택 지원
- `PC`, `Wide_PC`, `Mobile` 기준 변환
- 변환 결과 즉시 확인 및 복사 가능

## Tech Stack

- HTML
- CSS / SCSS
- JavaScript

## Run

`index.html` 파일을 브라우저에서 바로 열면 됩니다.
